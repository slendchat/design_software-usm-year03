## Изучение и примеры контекста с динамическими свойствами

Во многих библиотеках и фреймворках используется механизм **контекста с динамическими свойствами** —  
это специальная структура данных (обычно словарь), в которую можно добавлять произвольные пары «ключ–значение» во время выполнения программы.  
Такая структура позволяет библиотеке работать с данными, которые **ей заранее неизвестны**,  
и безопасно передавать дополнительную информацию между компонентами системы.

---

### 🔹 Примеры использования в существующих библиотеках

| Пример | Где используется | Что хранит |
|---------|------------------|-------------|
| **`AuthenticationProperties.Items`** | ASP.NET Core | Дополнительные параметры при аутентификации пользователя |
| **`HttpContext.Items`** | ASP.NET Core middleware | Данные, передаваемые между промежуточными обработчиками запроса |
| **`ActionExecutingContext.ActionParameters`** | ASP.NET MVC | Параметры метода контроллера при вызове |
| **`ValidationContext.RootContextData`** | FluentValidation | Дополнительные данные при валидации модели |
| **`IResolverContext.ContextData`** | HotChocolate (GraphQL) | Данные, доступные во время выполнения запроса |
| **`Behaviors`** | WPF | Дополнительное поведение, привязанное к элементам интерфейса |

---

### 🔹 Смысл и назначение

Механизм динамического контекста нужен, когда библиотека должна:
- принимать и передавать данные, о которых она **не знает заранее**;
- быть **расширяемой** — пользователи могут добавлять свои свойства без изменения исходного кода;
- хранить **данные запроса, пользователя или промежуточные результаты**, не нарушая изоляцию компонентов.

---

### Почему FatStruct не подходит
Библиотека не знает заранее, какие свойства захотят добавить пользователи.
FatStruct требует заранее описать все поля, поэтому библиотека не сможет расширяться.

---

